运行构建线程py（保险起见，最开头写入B.txt为0，设置A.txt为0，删除A、B文件夹中所有文件）
//A.txt为0时录音，为1时录音停止
//B.txt为1时检查分贝并推流，为0时停止

时刻运行检查分贝py。（1）每0.5秒检查当前目录下文件夹“A”中是否有新的文件产生，如果产生新的文件，（检查是否被允许检查分贝）检查B.txt文件，如果是1，运行检查分贝的代码，符合分贝后将文件移动到B，重命名将A（数字）.wav修改为B（数字）.wav，回到（1）													如果是0，删除文件，回到（1）	不符合分贝则删除
																												

									如果没有新的文件产生，则回到（1）（完成）

时刻运行录音py。每时每刻都在录音存文件 A（数字）.wav（完成）
时刻运行总py：

写入B.txt为1


（4）每秒检查B文件夹文件数，当文件数大于0时，（5）每秒检查文件数是否超过60个文件，如果超过60个文件，执行（3）
				当文件数等于0时，回到（4）					如果没有超过60个文件，检查文件数是否有变化，如果有变化，F=0，回到（5）
																				如果无变化，F+1，检查i是否大于或等于3，如果为大于等于3，执行（3）																										如果不为大于等于3，回到（5）

（3）写入B.txt为0，将所有B（数字）.wav合并为B.wav，并删除B文件夹内所有文件
